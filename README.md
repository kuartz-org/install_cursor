This script will install Cursor on your Linux machine with a script
allowing your `cursor` command to match the latest version of Cursor installed.


```bash
curl -sSL https://links.kuartz.fr/install_cursor | bash
```
